/**
 * Workspace Scripts
 *
 * Add utility scripts here for tasks like database seeding,
 * data migrations, or one-off maintenance operations.
 *
 * Run scripts with: `pnpm --filter @workspace/scripts run <script-name>`
 */
export {};
